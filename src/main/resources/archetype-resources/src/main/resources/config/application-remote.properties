
spring.datasource.url: jdbc:mysql://${DATABASE_KUBE_SRV}/?useUnicode=true&useJDBCCompliantTimezoneShift=true&useLegacyDatetimeCode=false&serverTimezone=CET
spring.datasource.username: ${DATABASE_USER}
spring.datasource.password: ${DATABASE_PASSWORD}
spring.datasource.driver-class-name: com.mysql.cj.jdbc.Driver

spring.flyway.enabled=false

## ACTUATOR
management.endpoint.configprops.enabled=true
management.endpoint.env.enabled=true
management.endpoints.web.exposure.include=health,info,configprops,env
